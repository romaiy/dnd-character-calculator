import { Flex, Stack, Text } from "@mantine/core";

const CharacterInfo = () => {

  return (
    <Stack bg={'#ffff'} p={40} style={{borderRadius: '16px'}} spacing={24}>
      <Text></Text>
      <Flex gap={16}>123</Flex>
    </Stack>
  );
}

export default CharacterInfo;